<div class="bulletpoint bulletpoint--attachment" data-decreto-node-id="{{ node.id }}">

  <!-- Begin - bar -->
  <div class="bulletpoint__bar">



    <!-- Begin - meta -->
    <div class="bulletpoint__meta  js-bulletpoint-toggle-bulletpoint">
      {{ label }}

      <!-- Begin - icon list -->
      <div class="action-list">

        {# Begin - notes #}
        {% if content['#contains_notes'] %}
          <button class="action-list__button action-list__button--info" tabindex="-1">
          <span class="material-icons"
                title="{{ 'This bullet point attachment has notes' | trans }}"
                data-toggle="tooltip">chat</span>
          </button>
        {% endif %}
        {# End - notes #}

        {# Start - closed #}
        {% if node.field_decreto_bpa_closed.value %}
          <button class="action-list__button action-list__button--info" tabindex="-1">
            <span class="material-icons"
                  title="{{ 'Closed bullet point attachment' | trans }}"
                  data-toggle="tooltip">
              lock
            </span>
          </button>
        {% endif %}
        {# End - closed #}

        {# Start - personal #}
        {% if node.field_decreto_bpa_personal.value %}
          <button class="action-list__button action-list__button--info" tabindex="-1">
            <span class="material-icons"
                  title="{{ 'Personal bullet point attachment' | trans }}"
                  data-toggle="tooltip">
              person
            </span>
          </button>
        {% endif %}
        {# End - personal #}

      </div>
      <!-- End - icon list -->

    </div>
    <!-- End - meta -->

    <!-- Begin - actions -->
    <div class="action-list">

      <!-- Begin - delete -->
      {% if content['#access']['canDelete'] %}
        <a
          href="{{ path('decreto_content_modify.bpas_delete', {'bullet_point_attachment': node.id()}) }}"
          title="{{ "Delete bullet point attachment" | trans }}"
          data-toggle="tooltip"
          class="action-list__button use-ajax" data-dialog-type="modal">
          <span class="material-icons">remove_circle</span>
        </a>
      {% endif %}
      <!-- End - delete -->

      <!-- Begin - edit -->
      {% if content['#access']['canEdit'] %}
        <a
          href="{{ path('decreto_content_modify.bpas_edit', {'bullet_point_attachment': node.id()}) }}"
          title="{{ "Edit bullet point attachment" | trans }}" class="action-list__button use-ajax"
          data-toggle="tooltip"
          data-dialog-type="modal">
          <span class="material-icons">edit</span>
        </a>
      {% endif %}
      <!-- End - edit -->

    </div>
    <!-- End - actions -->

  </div>
  <!-- End - bar -->

  <!-- Begin - content -->
  <div class="bulletpoint__content">

    {% if node.field_decreto_bpa_html.entity.uri.value %}
      <a
        href="{{ path('decreto_content_modify.bpas_modal', {'bullet_point_attachment': node.id()}) }}"
        title="{{ "Open in popup" | trans }}" class="btn btn-secondary btn-sm use-ajax"
        data-dialog-type="modal">Open in popup</a>
    {% elseif node.field_decreto_bpa_file.entity.uri.value %}
      {{ 'Download file:' | trans }} {{ content.field_decreto_bpa_file }}
    {% else %}
      <div>
        {{ content }}
      </div>
    {% endif %}

  </div>
  <!-- End - content -->

</div>
