{#
/**
 * @file
 * Themes the decreto context menu.
 *
 * Available variables:
 * - attributes: The HTML attributes for the containing element.
 * - link: A link to the file.
 * - icon: An icon.
 * - icon_only: Flag to display only the icon and not the label.
 * - icon_position: Where an icon should be displayed.
 *
 */
#}
{{ attach_library('decreto_context_menu/decreto-stretchy-navigation') }}

{% if links %}
  <div class="nav-container">
    <nav class="cd-stretchy-nav edit-content">
      <a class="cd-nav-trigger" href="#0">
        Menu
        <span aria-hidden="true"></span>
      </a>
      <ul>
        {% for link in links %}
          {#classes#}
          {% set classes = "" %}
          {% for cssClass in link.cssClass %}
            {% set classes = classes ~ ' ' ~ cssClass %}
          {% endfor %}

          {#data-#}
          {% set data = "" %}
          {% for dataName, dataAttr in link.data %}
            {% set data = data ~ ' data-' ~ dataName ~ '=' ~  dataAttr %}
          {% endfor %}

          <li><a href="{{ link.path }}" class="{{ classes }}"{{ data }}><span>{{ link.name }}</span></a></li>
        {% endfor %}
      </ul>

      <span aria-hidden="true" class="stretchy-nav-bg"></span>
    </nav>
  </div>

{% endif %}